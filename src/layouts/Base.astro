---
interface Props {
  title: string;
}

const { title } = Astro.props;
const videoProps = {
  autoplay: true,
  loop: true,
  muted: true,
  id: "some-id",
};

const sourceProps = {
  src: "public/vecteezy_military-combat-aircraft_7071482_127.mp4",
  type: "video/mp4",
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link
      rel="icon"
      type="image/svg+xml"
      href="/Screenshot 2023-10-19 at 3.03.55 PM.png"
    />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta charset="utf-8" />
    <link rel="shortcut icon" type="image/x-icon" href="" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" title="Joshua Conley Portfolio" />
    <!-- {{ vite src="resources/js/site.js|resources/css/site.css" }}
    {{# <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet"> #}} -->
    <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"
    ></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css"
    />
    <title>Joshua P. Conley</title>
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"
    ></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Pirata+One&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"
    ></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  </head>
  <body>
    <video {...videoProps}>
      <source {...sourceProps} />
      <!-- Include additional source elements for different video formats if needed -->
    </video>
    <slot />
  </body>
</html>
<script></script>
<style>
  .particle-wrapper {
    will-change: opacity, transform;
    opacity: 0.56405;
    transform: translate3d(0px, 0px, 0px) scale3d(2.15393, 2.15393, 1)
      rotateX(0deg) rotateY(0deg) rotateZ(0deg) skew(0deg, 0deg);
    transform-style: preserve-3d;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, #ffffff, #000000);
    z-index: -1;
  }

  .particle-wrapper div {
    will-change: transform;
    transform-style: preserve-3d;
  }

  .hero {
    background-color: transparent;
    /* Set the background to transparent */
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    overflow: hidden;
    position: relative;
  }
  #background-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1; /* Place the video behind other content */
  }

  /* Create a black opacity overlay */
  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(
      0,
      0,
      0,
      0.5
    ); /* Adjust opacity here (0.5 = 50% opacity) */
    z-index: 1; /* Place the overlay above the video */
  }

  .gradient-text {
    background: linear-gradient(to right, #000000, #757575);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    text-align: center;
  }

  .text-underline {
    text-decoration: underline;
  }
  /* Apply Pirata One font to elements with a class of "pirata-font" */
  .pirata-font {
    font-family: "Pirata One", sans-serif;
  }
  /* CSS for the video container */
  #background-video {
    position: fixed;
    top: 0;
    left: 0;
    min-width: 100%;
    min-height: 100%;
    z-index: -1; /* Place it behind other content */
  }
  video {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    right: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    object-fit: cover;
  }
</style>
